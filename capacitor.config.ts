import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vaibhav.devsecops',
  appName: 'DevSecOps',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
